# =========================================================================
# 编排模板 — 只定义基线，测试用例内 inline 覆盖差异参数
#
#   make_plan("local", branch="br_xxx", cases=["tc1"])
#   make_plan("remote", parallel=8, suite="regression")
# =========================================================================

local:
  snapshot_id: "20250206B003"
  suite: smoke
  parallel: 4
  build_env_name: local_pc
  exe_env_name: sim_eda
  repo_names: [rtl_core]
  build_names: [rtl_build]
  upload:
    type: rsync
    target: "ci@10.0.1.100:/data/results"

remote:
  snapshot_id: "20250206B003"
  suite: smoke
  parallel: 4
  build_env_name: remote_server_b
  exe_env_name: sim_eda_remote
  repo_names: [rtl_core]
  build_names: [rtl_build]
  upload:
    type: rsync
    target: "ci@10.0.2.200:/data/results"
