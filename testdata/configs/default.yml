# 默认测试套件定义
# 每个用例通过 args.cmd 指定要执行的 shell 命令

suite_name: default
description: "默认冒烟测试套件"

testcases:
  - name: sanity_check
    timeout: 60
    tags: [smoke, sanity]
    args:
      cmd: "echo '健全性测试通过'"

  # 添加更多用例示例：
  # - name: conv2d_basic
  #   timeout: 3600
  #   tags: [conv, basic]
  #   args:
  #     cmd: "make run_conv2d"
